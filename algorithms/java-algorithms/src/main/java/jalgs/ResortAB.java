package javaAlgorithms;

class ResortAB {
    // Complete the solve function below.
    // https://jsbin.com/xidotalivo/edit?js
    // resort de teste AB simples, caiu em B não volta pra A nem pra C, caiu em A pode ir pra B mas não volta pra C...
    // e a saída de cada variante tem que respeitar a última probabilidade (upper pq é só crescente)
    static long resortAB() {
        return 1;
    }
}
